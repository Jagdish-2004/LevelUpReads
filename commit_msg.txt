feat: production-grade favourites system with write-back caching

- Dedicated Favourite MongoDB model (one doc per user, unique userId index)
- Redis write-back cache: update Redis first, return immediately
- BullMQ write-behind worker: deduplicated 3s delayed DB sync per user  
- Split API routes: GET /api/favorites, POST /api/favorites/add, DELETE /api/favorites/remove
- favouriteCache.ts helper: getCachedIds, setCachedIds, add, remove, invalidate
- FavouritesSection, BookCard, BookDetails all use new endpoints
- Favourites persist across reloads via MongoDB, served fast via Redis
